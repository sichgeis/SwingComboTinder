node_modules/
dist/
coverage/
.task/
.DS_Store
.env
.env.local
# Generation runs, metadata, and archived masters stay local. Only the promoted
# production master is versioned so clean checkouts can build the web app.
figures/*/*/generated/**
!figures/*/*/generated/current.png
